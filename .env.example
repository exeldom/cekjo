# Set melalui Variables Railway. Aplikasi tidak otomatis membaca file .env.
DATA_DIR=/data
COOKIE_SECURE=1
# Opsional: jika tidak diisi, secret dibuat otomatis dan disimpan di volume data.
# SECRET_KEY=ganti-dengan-string-acak-panjang
